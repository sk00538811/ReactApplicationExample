var rootElement = React.createClass({ 
     render: function() {
        return React.DOM.div(null, 
            "new div generate: to check please ispact element in browser" 
        );
    }
});

//React.DOM.div({ id: "h1text", className: "btn btn-primary" }, "surendra" );